<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
</head>
<body>
    <h1><%=category%> products!</h1>
    <ul>
        <% for(let p of products) { %>
                <li><a href="/products/<%=p._id%>"><%=p.name%></a>
                </li>
        <% } %>
    </ul>
    <a href="/products/new">Add Product</a>
</body>
</html>